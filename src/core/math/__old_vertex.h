#ifndef VERTEX_H_
#define VERTEX_H_

#include "core/ehm/result.h"

// epoch math vertex
// epoch/src/core/math/vertex.h

#include "point.h"
#include "color.h"

class vertex {
public:
	//point<TValue, dimension> m_ptPosition;
	//color m_cColor;
	//vector<TValue, dimension> m_vNormal;
	//point<float, 2> m_uvcoord;
	//vector<TValue, dimension> m_vTangent;
	//vector<TValue, dimension> m_vBitangent;

public:
	vertex() = default;
	~vertex() = default;

};

#endif // !VERTEX_H_