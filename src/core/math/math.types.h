#ifndef MATH_TYPES_H_
#define MATH_TYPES_H_

// epoch core math types
// epoch/src/core/math/math.types.h
// This is just a good way to pre-define types

template<typename TValue, int N> class point;
template<typename TValue, int N, int M> class matrix;

template<typename TValue> class rectangle;

#endif // !MATH_TYPES_H_