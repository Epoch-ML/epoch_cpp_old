# epoch/src/junk/CMakeLists.txt : CMake configuration file for junk folder

# Junk is a collection of PoC and test projects that should only ever 
# be used for validation

# Projects
add_subdirectory(helloworld)
add_subdirectory(ProjectEuler)